<center>
  <h3>Edit <%= @channel.name %></h3>
  <div class="container">
    <%= simple_form_for [@study, @channel] do |f| %>
     <%= f.input :name %>
     <%= f.submit %>
    <% end %>
  </div>
</center>
