<div class="notifications-card-content">
  <% notifications.reverse_each do |notification| %>
    <div class="notifications-body-content row">
      <div class="teacher-avatar-container col-xs-1">
        <%= image_tag avatar_url(notification.message.user), class: "teacher-avatar" %>
      </div>
      <div class="col-xs-9 notification-message-container">
        <p><a class="notification-user"><%= notification.message.user.name %></a> posted a new message in <a class="notification-channel"><%= link_to notification.message.channel.name, channel_path(notification.message.channel) %> (<%= link_to notification.message.channel.subject.name, study_path(notification.message.channel.study) %>)</a><span class="notification-date">・<%= notification.message.created_at.strftime('%d %b %Y at %H:%M') %></span></p>
        <i class="fa fa-pencil" aria-hidden="true" style="font-size: 1.7rem; color: #2AB27B;">&nbsp;</i>&nbsp;<p class="notification-message" style="display: inline;"><%= notification.message.content %></p>
      </div>

      <div class="col-xs-2" style="text-align: left;">
      <% unless notification.status %>
        <p>
          <%= link_to change_status_notification_path(notification), class: "pull-right", method: :patch do%>
            <span style="margin-right: 0.5rem; margin-top: 0.4rem; color: #CC3232;">Archive&nbsp;&nbsp;<i class="fa fa-trash-o" aria-hidden="true"></i></span>
          <% end %>
        </p>
      <% end %>
      </div>
    </div>
  <% end %>
</div>
