<div class="container">
  <div class="row">
    <div class="col-xs-12 col-sm-10 col-sm-offset-1 col-md-8 col-md-offset-2 col-lg-6 col-lg-offset-3">
      <div class="row form-card">
        <div class="col-xs-10 col-xs-offset-1 col-sm-8 col-sm-offset-2">

          <div class="form-card-header text-center">
            <h1>New Study</h1>
            <p><span>Create a study</span> for your students.</p>
          </div>

          <div class="form-card-content">
            <%= simple_form_for @study do |f| %>

              <div class="input form-group-lg">
                <%= f.input :subject_id, as: :select, collection: Subject.all %>

              </div>

              <div class="input form-group-lg">
                <%= f.input :CSV_File, as: :file %>

              </div>

              <div class="form-actions">
                <%= f.button :submit, "Continue →", class: "btn submit-btn" %>
              </div>

            <% end %>
          </div>

        </div>
      </div>
    </div>
  </div>
</div>
